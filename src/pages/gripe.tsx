import React, {ReactElement, JSXElementConstructor, ReactFragment, ReactPortal, FunctionComponent} from "react"
import {LinkProps} from "next/dist/client/link";


export const Gripe =  (
  {children}
                               ) => (
    <>{children}</>
);

export default Gripe
